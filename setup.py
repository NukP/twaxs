from setuptools import setup, find_packages

setup(
    name="sxrd",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here, e.g.
        # 'numpy>=1.20.0'
    ],
)
